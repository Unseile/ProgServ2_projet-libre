# Active le moteur de r̩̩criture d'URL.
# Permet de rediriger les requ̻tes vers d'autres emplacements.
RewriteEngine On

# Blocage d'acc̬s aux r̩pertoires sensibles.
RewriteRule ^src/ - [R=404,L]

# Si le fichier ou le r̩pertoire demand̩ existe dans /public, ne pas r̩̩crire le chemin d'acc̬s (tout ce qui est dans /public reste accessible).
RewriteCond %{DOCUMENT_ROOT}/public/$1 -f [OR]
RewriteCond %{DOCUMENT_ROOT}/public/$1 -d

# Tout le reste des requ̻tes est redirig̩ vers /public en masquant /public dans l'URL.
RewriteRule ^(.*)$ public/$1 [L]
